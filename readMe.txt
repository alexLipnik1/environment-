
instructions: 

step 1:
in the terminal run the command "npm install" when you inside the project repository.

step 2: 
run the following commands: 

- "node script.js"
wait a little bit. the command will put the data in the mongodb in mydb port 27017, and will create the geo and tag collections.

- "npm run dev.client"
- "npm run dev.server".

step 3:
put the URL address "http://localhost:8080/"

step 4:
change the date and time as you wish and click the "send" button. 